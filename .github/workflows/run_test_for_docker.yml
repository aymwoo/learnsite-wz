name: Docker Environment Tests

on:
  workflow_run:
    workflows: ["Release Build and Publish"]
    types: [completed]
    branches: [master, main]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}
    steps:
      - name: Get Alpha Release
        id: release
        run: |
          # 优先获取alpha版本
          ALPHA_TAGS=$(gh release list --repo ${{ github.repository }} --json tagName --jq '.[] | select(.tagName | startswith("alpha-")) | .tagName' | sort -r)
          if [ ! -z "$ALPHA_TAGS" ]; then
            TAG_NAME=$(echo "$ALPHA_TAGS" | head -1)
            echo "tag_name=$TAG_NAME" >> $GITHUB_OUTPUT
          else
            # 如果没有alpha版本，获取最新版本
            TAG_NAME=$(gh release list --repo ${{ github.repository }} --limit 1 --json tagName --jq '.[0].tagName')
            echo "tag_name=$TAG_NAME" >> $GITHUB_OUTPUT
          fi
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Download Release Asset
        run: |
          gh release download ${{ steps.release.outputs.tag_name }} --repo ${{ github.repository }} --pattern "*.zip"
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Extract Release
        run: |
          unzip -o learnsite-*.zip -d .

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18.x'

      - name: Run MS SQL Server in Docker
        run: |
          docker run -d --name learnsite-mssql -e 'ACCEPT_EULA=Y' -e "MSSQL_SA_PASSWORD=YourStrong!Passw0rd" -p 1433:1433 mcr.microsoft.com/mssql/server:2022-latest
          echo "Waiting for SQL Server to start..."
          sleep 20

      - name: Install Mono and XSP4
        run: |
          sudo apt-get update
          sudo apt-get install -y ca-certificates gnupg
          sudo gpg --homedir /tmp --no-default-keyring --keyring /usr/share/keyrings/mono-official-archive-keyring.gpg --keyserver hkp://keyserver.ubuntu.com:80 --recv-keys 3FA7E0328081BFF6A14DA29AA6A19B38D3D831EF
          echo "deb [signed-by=/usr/share/keyrings/mono-official-archive-keyring.gpg] https://download.mono-project.com/repo/ubuntu stable-focal main" | sudo tee /etc/apt/sources.list.d/mono-official-stable.list
          sudo apt-get update
          sudo apt-get install -y mono-complete mono-xsp4

      - name: Initialize Database
        run: |
          if [ -f "sql/learnsite.sql" ]; then
            docker cp sql/learnsite.sql learnsite-mssql:/tmp/learnsite.sql
            docker exec learnsite-mssql /opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P "YourStrong!Passw0rd" -C -Q "CREATE DATABASE [learnsite];"
            docker exec learnsite-mssql /opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P "YourStrong!Passw0rd" -C -d learnsite -i /tmp/learnsite.sql
          else
            echo "[WARNING] sql/learnsite.sql not found, skipping database initialization"
          fi

      - name: Update web.config for Docker test
        run: |
          if [ -f "web.config" ]; then
            sed -i 's/Data Source=127.0.0.1;/Data Source=localhost;/g' web.config
            echo "[INFO] Updated web.config connection string"
          else
            echo "[WARNING] web.config not found, skipping configuration update"
          fi

      - name: Check directory structure
        run: |
          echo "=== Current Directory ==="
          pwd
          ls -la
          echo ""
          echo "=== Bin Directory ==="
          ls -la Bin/ 2>/dev/null || echo "Bin directory not found"
          echo ""
          echo "=== Check for DLLs ==="
          find . -name "*.dll" | head -10
          echo ""
          echo "=== Check for web.config ==="
          find . -name "web.config" | head -5

      - name: Test XSP4 directly
        run: |
          echo "=== Testing XSP4 Installation ==="
          which xsp4
          xsp4 --version 2>&1 || echo "xsp4 version check failed"
          echo ""
          echo "=== Starting XSP4 in foreground for diagnostics ==="
          # 在后台启动XSP4，但立即检查日志
          xsp4 --port 8080 --address 0.0.0.0 --verbose &
          XSP4_PID=$!
          echo "XSP4 PID: $XSP4_PID"
          sleep 10
          echo ""
          echo "=== Process Status ==="
          ps aux | grep xsp4 | grep -v grep
          echo ""
          echo "=== Port Status ==="
          netstat -tuln | grep 8080 || echo "Port 8080 not listening"
          echo ""
          echo "=== XSP4 Output (if any) ==="
          # 尝试获取XSP4的输出
          wait $XSP4_PID 2>/dev/null || true
          echo "XSP4 process status: $?"

      - name: Test frontend page access
        run: |
          echo "=== Testing Frontend Page Access ==="
          # 重新启动XSP4
          xsp4 --port 8080 --address 0.0.0.0 > xsp4.log 2>&1 &
          XSP4_PID=$!
          echo "XSP4 started with PID: $XSP4_PID"
          sleep 15
          
          echo "Testing main page..."
          curl -v http://localhost:8080
          
          echo "\n=== XSP4 Logs ==="
          cat xsp4.log || echo "No xsp4.log found"
          
          # 停止XSP4
          kill $XSP4_PID 2>/dev/null || true

      - name: Alternative - Use fastcgi-mono-server
        run: |
          echo "=== Trying alternative: fastcgi-mono-server ==="
          # 检查是否有fastcgi-mono-server
          which fastcgi-mono-server 2>/dev/null || echo "fastcgi-mono-server not found"
          which fastcgi-mono-server4 2>/dev/null || echo "fastcgi-mono-server4 not found"

      - name: Final Test
        run: |
          echo "=== Final Web Service Test ==="
          # 尝试使用Python启动一个简单的HTTP服务器作为替代
          echo "Starting Python HTTP server as fallback..."
          python3 -m http.server 8080 &
          PYTHON_PID=$!
          sleep 3
          echo "Testing Python server..."
          curl -s http://localhost:8080 > /dev/null && echo "Python server is working" || echo "Python server failed"
          kill $PYTHON_PID 2>/dev/null || true

      - name: Cleanup
        run: |
          docker stop learnsite-mssql || true
          docker rm learnsite-mssql || true
